<h1>Burgers</h1>

<h2>Uneaten Burgers</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{burger_name}}

      <button class="devour-burger" data-id="{{id}}" data-eaten="{{devoured}}">Eat Burger
      </button>
      <br>
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have died for our sins</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{burger_name}}
      <br>
    {{/if}}
  {{/each}}
</ul>

<h2>Create a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burgerName">Burger Name:</label>
    <input type="text" id="burgerName">
  </div>

  <button class="create_burger" type="submit">Add Burger</button>
</form>
